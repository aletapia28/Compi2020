let
	rec
		func doble(var i : Integer) : Integer ~
			triplicar(i)/3*2
! falta and
		proc triplicar(var i : Integer) ~
			i := i + doble(i)
		end
	end
in
	nil
end

! Error: Falta el "and" que separe los proc-funcs