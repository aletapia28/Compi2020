let
  var i : Integer;
  var n : Integer
in
  i := 0;
  getint(var n);
  repeat loop ciclo do
    if i > n
      then exit
    else
      i := i + 1;
      putint(i);
      puteol()
    end
  end
end
  
! OK